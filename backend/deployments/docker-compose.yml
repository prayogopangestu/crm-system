services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: crm
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm
    ports:
      - "5432:5432"
    volumes:
      - crm_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crm -d crm"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - crm_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  migrate:
    build:
      context: ..
      dockerfile: deployments/Dockerfile
    command: ["/app/migrate", "-direction", "up", "-dir", "/app/migrations"]
    environment:
      DATABASE_URL: postgres://crm:crm@postgres:5432/crm?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build:
      context: ..
      dockerfile: deployments/Dockerfile
    environment:
      APP_ENV: development
      APP_BASE_URL: http://localhost:3000
      APP_TIMEZONE: Asia/Jakarta
      HTTP_ADDR: :8080
      GRPC_ADDR: :9090
      DATABASE_URL: postgres://crm:crm@postgres:5432/crm?sslmode=disable
      REDIS_URL: redis://redis:6379/0
      JWT_SECRET: development-only-secret-change-this-32-bytes
      APP_ENCRYPTION_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
      CORS_ALLOWED_ORIGINS: http://localhost:3000
    ports:
      - "8080:8080"
      - "9090:9090"
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/readyz"]
      interval: 10s
      timeout: 3s
      retries: 10

volumes:
  crm_postgres:
  crm_redis:

